{
  "name": "haprial",
  "version": "1.0.0",
  "private": true,
  "description": "Haprial - A minimal static blog generator",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && cd dist && npx serve"
  }
}
